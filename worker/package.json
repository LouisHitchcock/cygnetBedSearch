{
  "name": "cygnet-bed-search-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:init": "wrangler d1 execute cygnet-bed-search-db --file=./schema.sql",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
